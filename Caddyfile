# Replace yourdomain.com with your actual domain (or use :80 for local HTTP only)
# Caddy automatically provisions and renews HTTPS certificates via Let's Encrypt

yourdomain.com {
  reverse_proxy app:3000
}
